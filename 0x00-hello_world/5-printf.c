#include <stdio.h>

/**
 * main - prints the string using printf function
 * Description - with proper grammar, but the outcome
 * is a piece of art,
 * Return: 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0)
}
