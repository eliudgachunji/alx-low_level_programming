#include "main.h"

/**
 *_strlen - returns the length of a string
 * @str: The string in question
 *
 * Return: The length of a string
 */

int _strlen(char *s)

{

 length = 0;

while (*str++)
length++;

return (length);

}
