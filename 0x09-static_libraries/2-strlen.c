#include "main.h"

/**
 *_strlen - returns the length of a string
 * @s: The string in question
 * Return: The length of a string
 */

int _strlen(char *s)

{

length = 0;

while (*s++)
length++;

return (length);

}
