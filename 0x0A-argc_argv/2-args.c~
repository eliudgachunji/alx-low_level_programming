#include "main.h"
#include <stdio.h>

/**
 * main - prints all arguments it receives
 * @argc: number of argumnets in command line
 * @argv: name of array
 * Return: 0
 */

int main(int argc, int char *argv[])

{

int i;

for (i = 0; i < argc; i++)
printf("%s\n", argv[i]);

return (0);

}
